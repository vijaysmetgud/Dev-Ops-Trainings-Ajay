apiVersion: v1
kind: Pod
metadata:
  name: hello-pod name of the pod
spec:
  containers:
    - name: hello-ctrl 
      image: Jenkins 
      ports: 
        - containerPort: 8080


---

apiversion: V1
Kind: Pod
metadata: 
   name: my-first-pod
spec:
  container:
  - name: my-first-container
    image: ngix
    ports:
    - container port: 80
    

   
